
<!DOCTYPE html>
<html>
    <head>
        <h1>Today</h1>
    </head>
    <body>
        
        <% @date = Time.now %>
        <p> The date is <%= @date.strftime("%A, %B%e, %Y") %></p>
       <p> The date is <%= Time.now.strftime("%A, %B%e, %Y") %></p>
       <p> The date is <%= Date.today.strftime("%A, %B %d, %Y") %></p>
    </body>
</html>